#include <iostream>

struct Entry {
    char label;
    int key;
    // struct Entry * next;
};

// imitating a Python list like [10, 12]:
int math() {
    // Entry n1 = {'A', 10, NULL};
    // Entry n2 = {'B', 12, Entry * n1};

    return 0;
}
